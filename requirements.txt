# ========================================
# Sketch-to-CAD Requirements (Latest Versions)
# ========================================

# Image Processing (Core)
opencv-python  # Latest version
numpy          # Latest compatible version
Pillow         # Latest version

# DXF Output
ezdxf          # Latest version

# AI API - GPT-5 (Primary)
openai==1.14.3 # Pin version to avoid proxy issues

# Optional AI APIs (for future extensions)
# anthropic              # Claude API
# google-generativeai    # Gemini API

# Environment
python-dotenv  # Load .env for API keys

# HTTP Client (for OpenAI)
httpx==0.27.0  # Pin version to match openai dependency
